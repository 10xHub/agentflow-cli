We need to work on our Onboarding process

When user start like this:

agentflow init

user should not pass any extra argument...


Then it will ask

What is your agent name? (default: MyAgent)
You want a quick Start or Production setup? (default: Quick Start)

IF user pick quick start, then we will copy from here
agentflow_cli/cli/templates/dev

(But we need as template so we can changes thing dynamically based on user answer)


IF user pick production, then we will ask few more question

1. What type of authentication do you want to use? (Options: JWT, Custom)
if JWT Then we need to use inside agnetflow.json there we need to keep as "auth": "jwt"

2. Do want to use rate limiting? (Options: No, Memory Based, Redis Based)

agentflow_cli/cli/templates/prod


# When done,

Lets use coding agent skills to build with agentflow quickly
Run agentflow skills command

Then:
We will install `google-genai`

And ask user to setup env file
and run agentflow play and enjoy